import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  buttonRegister: {
    borderColor: "#8367D8",
    borderRadius: 0,
    borderWidth: 3,
    marginTop: "5%",
    width: "100%",
    height: 45,
  },
});

import { makeStyles } from "@material-ui/core";

export const smallCard = makeStyles({
  root: {
    width: 250,
    maxWidth: 250,
    height: 350,
  },
  media: {
    maxWidth: 250,
    height: 250,
  },
});

import { makeStyles } from "@material-ui/core";

export const smallCard = makeStyles({
  root: {
    width: 200,
    maxWidth: 200,
    height: 350,
  },
  media: {
    maxWidth: 200,
    height: 250,
  },
});
